PyYAML>=6.0
# Only needed for the optional LLM path (run.py pipeline --llm). The heuristic
# extractor and the rest of the pipeline run without it.
boto3>=1.34
# Only needed to import a PDF/DOCX resume (run.py import-resume). Text/markdown
# resumes need neither.
pypdf>=4.0
python-docx>=1.1
